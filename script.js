const questions = [    {question: "Mang‘itlar davri qachon boshlanadi?", answers: ["XVIII asr", "XVI asr", "XIX asr"], correct: 0},    {question: "Asosiy savdo yo‘li qaysi?", answers: ["Ipak yo‘li", "Buyuk ipak yo‘li", "Savdo yo‘li"], correct: 1},];let currentQuestion = 0;function loadQuestion() {    document.getElementById("question-text").innerText = questions[currentQuestion].question;    document.querySelectorAll(".answer-btn").forEach((btn, index) => {        btn.innerText = questions[currentQuestion].answers[index];    });}function checkAnswer(index) {    if (index === questions[currentQuestion].correct) {        document.getElementById("result").innerText = "To‘g‘ri!";    } else {        document.getElementById("result").innerText = "Xato!";    }    currentQuestion++;    if (currentQuestion < questions.length) {        loadQuestion();    } else {        document.getElementById("question-container").innerHTML = "<p>Test yakunlandi!</p>";    }}window.onload = loadQuestion;